# Tool index

Generated by `scripts/gen_tools_index.py` from the first docstring line of
every script. 94 tools in `tools/`, 6 in `legacy/mmi3g-atlas/`.
All are Python 3 standard library only; every tool prints `--help`.
Docstrings are in English or Serbian depending on when the tool was written.

## Firmware extraction

Read-only readers for the QNX images that hold the navigation parsers.

| Tool | Purpose |
|---|---|
| `tools/fw_protection_model.py` | read-only mapiranje modela zastite u NavCore. |
| `tools/qnx6_extract.py` | Read-only QNX6 filesystem inspector/extractor. |

## MIB PSF60 source decoding

Reading the newer MIB map files (`PSF2DDTM`, version `60DREID4ADAS7`).

| Tool | Purpose |
|---|---|
| `tools/audiurban_spatial_feature_profile.py` | Profile MIB geometry flag bits against spatially matched Orion properties. |
| `tools/basic_dynamic_attributes.py` | Firmware-backed directory decoder for Basic dynamic edge attributes. |
| `tools/basic_dynamic_attributes_profile.py` | Profile Basic dynamic edge-attribute directories over the full PSF corpus. |
| `tools/basic_geometry_decode.py` | Decode firmware-confirmed MIB Basic edge geometry into normalized polylines. |
| `tools/basic_geometry_grammar.py` | Infer and validate the nested subrecord grammar inside Basic geometry records. |
| `tools/basic_graph_export.py` | Export a validated MIB Basic routing graph for the Orion/ATLAS adapter. |
| `tools/basic_handle2_directory.py` | Validate the firmware-defined Basic handle-2 edge directory. |
| `tools/basic_handle2_name_profile.py` | Profile Basic handle-2 name identifiers, scripts and ordering. |
| `tools/basic_handle2_probe.py` | Inventory and rank structural candidates in every Basic handle-2 payload. |
| `tools/basic_handle2_text_decode.py` | Decode and validate direct Basic handle-2 SDString sequences. |
| `tools/basic_name_semantics.py` | Language, transliteration and display selection for Basic SDStrings. |
| `tools/basic_road_attributes.py` | Firmware-backed primitives for MIB Basic road attributes. |
| `tools/basic_road_attributes_profile.py` | Profile Basic road-attribute bytes over the complete PSF corpus. |
| `tools/basic_semantic_probe.py` | Infer and validate the first semantic layer of MIB PSF60 Basic clusters. |
| `tools/basic_world_country_languages.py` | Decode and validate Basic PSF world-country official-language lists. |
| `tools/mib_graph_spatial_extract.py` | Extract a compact MIB Basic edge corpus intersecting a WGS84 bbox. |
| `tools/pre_writer_layers.py` | Lossless PSF60 AdvancedRouting and ADAS cluster decoders. |
| `tools/pre_writer_layers_export.py` | Export lossless per-edge AdvancedRouting/ADAS source before Orion writing. |
| `tools/pre_writer_layers_profile.py` | Full-corpus structural profiler for the PSF60 layers before Orion writing. |
| `tools/psf_decode.py` | Incremental, read-only decoder for Audi MIB PSF v60 map files. |

## Original 3G Plus reference profiling

Read-only profilers of the original Orion `.ATLAS` databases used as the writer reference.

| Tool | Purpose |
|---|---|
| `tools/orion_block_header_profile.py` | read-only profil ATLAS block headera. |
| `tools/orion_block_key_spatial_probe.py` | da li je kljuc bloka prostorni? |
| `tools/orion_centerline_geometry_decode.py` | Decode original Orion clothoid Parts and PointLld rows per edge. |
| `tools/orion_edge_geometry_decode.py` | Reconstruct original Orion edge endpoints and geometry provenance. |
| `tools/orion_edge_property_decode.py` | Decode original Orion PropertyD1 lists and attach them to graph edge rows. |
| `tools/orion_graph_spatial_probe.py` | Locate original Orion graph chunks intersecting a longitude/latitude box. |
| `tools/orion_index_block_profile.py` | profil `INDEX` i `REVISION` blokova. |
| `tools/orion_item_identifier_profile.py` | Profile original Orion Item.Identifiers without assuming edge-ID semantics. |
| `tools/orion_layer_survey.py` | dekoduje šeme sadržaja svakog Orion .ATLAS sloja. |
| `tools/orion_lzma_failure_probe.py` | sta je sa blokovima koji se ne raspakuju. |
| `tools/orion_mib_bounded_graph_match.py` | Find bounded MIB graph paths along an original Orion edge corridor. |
| `tools/orion_mib_corridor_match.py` | Match Orion edges to one or more MIB segments by corridor coverage. |
| `tools/orion_mib_direction_profile.py` | Cross-tab raw Orion Property orientation values with MIB path direction. |
| `tools/orion_mib_name_identity_profile.py` | Cross-tab repeated Orion Item identifiers against MIB names/class codes. |
| `tools/orion_mib_property_value_profile.py` | Profile decoded Orion Property values against accepted MIB graph paths. |
| `tools/orion_mib_spatial_match.py` | Spatially match decoded original Orion edges to compact MIB candidates. |
| `tools/orion_mib_stable_attribute_profile.py` | Compare stable attributes on accepted Orion-to-MIB topology paths. |
| `tools/orion_mib_topology_chain.py` | Validate Orion-to-MIB corridor candidates as connected topology chains. |
| `tools/orion_nonkey_block_profile.py` | sta su blokovi bez kljuca (sentinel C). |
| `tools/orion_property_corpus_profile.py` | Profile PropertyD1 lists and Property subclasses across an Orion ATLAS. |
| `tools/orion_property_layout_profile.py` | Prove PropertyD1/Attributes cardinalities in an extracted Orion graph chunk. |
| `tools/orion_psd_reference_profile.py` | Profile the original MMI 3G Plus Orion PSD catalog as a writer reference. |
| `tools/orion_schema_extract.py` | Extract self-contained logical/physical schema samples from Orion ATLAS. |
| `tools/orion_schema_name_inventory.py` | Inventory logical schema names across an Orion ATLAS in parallel. |
| `tools/orion_tile_grid_probe.py` | formula tile ID-a (C), putanje (B) i nivoa (A). |
| `tools/orion_uncovered_block_probe.py` | blokovi koje INDEX listovi ne pokrivaju. |
| `tools/orion_vidtable_identifier_profile.py` | Compare raw 64-bit VidTable.AtlasIds with graph Item.Identifiers. |
| `tools/orion_vidtable_row_mapping.py` | Decode and validate VidTable row-to-AtlasId/XacVectorOffset mappings. |
| `tools/orion_xac_payload_layout_infer.py` | Infer observed payload cursor branches per static XAC descriptor class. |
| `tools/orion_xac_type_table_profile.py` | Export the active NavCore XAC descriptor table and observed key counts. |
| `tools/orion_xac_vector_bind.py` | Bind ordered Orion VidTable chunks to physical XAC VEKTORBLOCK records. |
| `tools/orion_xac_vector_branch_profile.py` | Profile runtime XAC payload branches by static and dynamic descriptors. |
| `tools/orion_xac_vector_branch_select.py` | Select dominant runtime XAC branches for semantic parser implementation. |
| `tools/orion_xac_vector_cursor_validate.py` | Validate structural XAC cursors against ordered physical targets. |
| `tools/orion_xac_vector_name_refs.py` | Extract XAC name references using NavCore's get_names_of_vector grammar. |
| `tools/orion_xac_vector_offset_resolve.py` | Resolve VidTable XacVectorOffsets through the firmware-defined XAC header modes. |
| `tools/orion_xac_vector_payload_probe.py` | Collect bounded payload samples per XAC descriptor class in parallel. |
| `tools/orion_xac_vector_record_layout.py` | Decode the firmware-defined structural layout of 0xc0 XAC records. |
| `tools/orion_xac_vector_record_profile.py` | Profile firmware-defined 0xc0 XAC vector record headers in parallel. |
| `tools/orion_xac_vector_spacing_profile.py` | Profile spacing between ordered XAC vector targets using parallel workers. |

## Orion column codec and object writers

Column codec primitives and the decoded-chunk writers that produce Orion objects from MIB source rows.

| Tool | Purpose |
|---|---|
| `tools/orion_centerline_writer.py` | Build a self-validating decoded Orion clothoid centerline graph chunk. |
| `tools/orion_clothoid.py` | Geometry-neutral Orion clothoid source primitives. |
| `tools/orion_clothoid_export.py` | Validate and export the pre-writer Orion clothoid source layer. |
| `tools/orion_column_codec.py` | Verified low-level primitives for original MMI 3G Orion column codecs. |
| `tools/orion_merged_graph_writer.py` | Build one self-validating decoded Orion topology and centerline graph. |
| `tools/orion_object_writer.py` | Build self-validating decoded Orion point and graph objects from MIB JSONL. |

## ATLAS container: decode, verify, write

HEADER / REVISION / INDEX / CONTAINER grammar, the spatial block key, block round-trip and the full-file assembler.

| Tool | Purpose |
|---|---|
| `tools/orion_atlas_assemble.py` | sastavljac kompletne ATLAS baze (svi delovi). |
| `tools/orion_atlas_header_decode.py` | dokazana specifikacija ATLAS `HEADER` bloka. |
| `tools/orion_block_grammar_verify.py` | svaki bajt ATLAS bloka mora biti objasnjen. |
| `tools/orion_block_writer.py` | sastavljac ATLAS bloka i njegov round-trip dokaz. |
| `tools/orion_cell_chunk_writer.py` | CONTAINER blok po celiji iz MIB izvora. |
| `tools/orion_cell_partition.py` | particionisanje MIB grafa u celije Orion stabla. |
| `tools/orion_cells_geojson.py` | celije blokova kao GeoJSON pravougaonici. |
| `tools/orion_conf_write.py` | `.conf` opis ATLAS fajla za pkgdb. |
| `tools/orion_container_header_decode.py` | bajt-po-bajt spec CONTAINER bloka. |
| `tools/orion_index_decode.py` | dekoder i validator ATLAS `INDEX` direktorijuma. |
| `tools/orion_index_root_decode.py` | koren INDEX stabla i REVISION blok. |
| `tools/orion_tile_formula_verify.py` | egzaktna formula kljuca bloka iz geometrije. |

## Repeatable Ghidra evidence runners

Headless Ghidra batches that regenerate the firmware evidence quoted in the docs.

| Tool | Purpose |
|---|---|
| `tools/run_basic_geometry_re.py` | Run the repeatable Ghidra batch used for Basic geometry reconstruction. |
| `tools/run_basic_handle2_re.py` | Run the repeatable sequential Ghidra batch for Basic handle-2 semantics. |
| `tools/run_basic_road_attributes_re.py` | Run the repeatable Ghidra batch for Basic road-attribute reconstruction. |
| `tools/run_basic_urban_semantics_re.py` | Reproduce the local firmware proof for the MIB edge Urban flag. |
| `tools/run_ghidra_sharded_grep.py` | Run bounded Ghidra decompiler grep in parallel, then merge its artifacts. |
| `tools/run_orion_column_codec_re.py` | Extract repeatable Orion column-codec evidence from the MMI 3G NavCore project. |
| `tools/run_pre_writer_layers_re.py` | Run the repeatable Ghidra batch for AdvancedRouting and ADAS semantics. |
| `tools/run_psd15_profile_re.py` | Extract repeatable ADAS-interface -> PSD1.5 profile conversion evidence. |

## Stage runners and orchestration

Full-corpus stages with cross-checks, plus the JSON-manifest pipeline runners.

| Tool | Purpose |
|---|---|
| `tools/orion_stage_watcher.py` | Run a JSON-defined stage pipeline and persist completion state. |
| `tools/run_basic_identifier_crosscheck.py` | Cross-check language IDs against Albania and Bosnia Basic PSFs in an archive. |
| `tools/run_basic_name_stage.py` | Run the complete, self-validating Basic language/name stage. |
| `tools/run_basic_road_attributes_stage.py` | Run and cross-check the complete Basic road-attribute source stage. |
| `tools/run_container_pipeline.py` | orkestrator za container/ATLAS sloj. |
| `tools/run_orion_cross_version_corpus.py` | Run one resumable Orion-to-MIB spatial corpus stage end to end. |

## Legacy MMI 3G Plus research tools

The original Orion/ATLAS reverse-engineering scripts (August 2026). Kept as they were;
their story is in `legacy/mmi3g-atlas/DOCS.md`.

| Tool | Purpose |
|---|---|
| `legacy/mmi3g-atlas/atlas_bits.py` | bitovni čitač za Orion kolone tipa 3. |
| `legacy/mmi3g-atlas/atlas_blocks.py` | parser blok-strukture .ATLAS (Orion) kontejnera. |
| `legacy/mmi3g-atlas/atlas_export.py` | izvoz tačaka iz `.ATLAS` (PSD) u GeoJSON. |
| `legacy/mmi3g-atlas/atlas_recon.py` | prva faza reverse engineeringa binarnog formata. |
| `legacy/mmi3g-atlas/ifs_tool.py` | raspakivanje QNX IFS image-a iz MMI 3G+ firmware paketa (MU9411). |
| `legacy/mmi3g-atlas/nds_names.py` | vadi imena (ulice, mesta) iz NDS `.psf` fajla. |

## Pipeline manifests

| File | Purpose |
|---|---|
| `tools/orion_container_pipeline.json` | 9-stage container verification pipeline (header, grammar, index, root, block round-trip, spatial key, formula, grid, assembler round-trip) for `run_container_pipeline.py`. |
| `tools/orion_xac_pipeline.json` | XAC vector/name bridge stages for `orion_stage_watcher.py`. |

## Ghidra scripts

`ghidra_scripts/*.java` are generic headless helpers (function listing, decompile, xrefs, pointer tables, string xrefs)
used by the `run_*_re.py` runners. `legacy/mmi3g-atlas/ghidra_scripts/*.java` are the earlier NavCore probes.
